# Local editor / agent state
.cursor/

# OS
.DS_Store
Thumbs.db

# env / secrets — keep .env.example tracked
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.secret
*.token
credentials.json
**/secrets/
.npmrc
auth.json

# Graph / Substreams CLI local auth
.graph-cli/
.subgraph/
.substreams/

# local / scratch (root-level only; do not match .github/workflows)
hackathon/
/workflows/

# Rust / Substreams build outputs
**/target/
**/*.spkg
**/*.wasm
**/*_pb.rs

# Local Substreams / sink run artifacts
**/*.jsonl
**/*.err
**/*.log
substreams/replay.log

# Node
node_modules/

# Generated Substreams subgraph build outputs
substreams/build/
