---
title: Creating workflows
description: Build on the canvas, template fields, and test before you list.
---

Click **Add step** to drop an action. Select an integration and fill its fields.

Use `{{NodeName.field}}` to pass outputs forward. Test with **Run** before you open **Marketplace**. Listing requires at least one successful run, and paid listings require a linked Privy wallet.
