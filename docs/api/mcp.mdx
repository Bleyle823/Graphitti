---
title: MCP
description: Install the Graphitti catalog server in Cursor or Claude.
---

App-wide server:

```
https://<host>/mcp
```

Single listing:

```
https://<host>/mcp/w/{slug}
```

Tools: `search_workflows` and `call_workflow`. Optional `Authorization: Bearer wfb_...` for owner bypass.
