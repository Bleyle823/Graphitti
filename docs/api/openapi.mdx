---
title: OpenAPI
description: Machine-readable catalog at /openapi.json.
---

`GET /openapi.json` and `GET /api/openapi` return OpenAPI 3.1 with `search_workflows`, `call_workflow`, and per-slug call operations.

Use that document to generate an HTTP client. Graphitti does not ship an npm SDK.
