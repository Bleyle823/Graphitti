---
title: API keys
description: Owner-scoped wfb_ keys shown once.
---

Create keys under **API Keys**. Graphitti stores a hash and shows the `wfb_` secret once.

Keys are owner-scoped. They can execute your workflows by id and bypass the marketplace payment gate on listings you own. They are not org keys.
