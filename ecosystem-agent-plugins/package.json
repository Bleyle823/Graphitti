{
  "name": "@graphitti/ecosystem-agent-plugins",
  "private": true,
  "description": "Ecosystem agent plugins for The Graph and Privy protocol tool groups",
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm --filter @bleyle823/graph-core test && pnpm --filter @bleyle823/privy-core test && pnpm --filter plugin-the-graph test && pnpm --filter plugin-privy test",
    "type-check": "pnpm -r run type-check",
    "publish:cores": "pnpm --filter @bleyle823/graph-core publish --no-git-checks && pnpm --filter @bleyle823/privy-core publish --no-git-checks"
  }
}
