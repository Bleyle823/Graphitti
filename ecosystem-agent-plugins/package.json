{
  "name": "@graphitti/ecosystem-agent-plugins",
  "private": true,
  "description": "Ecosystem agent plugins for The Graph and Privy protocol tool groups",
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm --filter @graphitti/graph-core test && pnpm --filter @graphitti/privy-core test && pnpm --filter plugin-the-graph test && pnpm --filter plugin-privy test",
    "test:live": "pnpm --filter @graphitti/graph-core exec vitest run src/live.integration.test.ts && pnpm --filter @graphitti/privy-core exec vitest run src/live.integration.test.ts",
    "type-check": "pnpm -r run type-check",
    "publish:cores": "pnpm --filter @graphitti/graph-core publish --no-git-checks && pnpm --filter @graphitti/privy-core publish --no-git-checks"
  }
}
